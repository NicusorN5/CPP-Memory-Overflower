///Example of usage

#include "memory_overflow.h" //Include the very little header that contains our functions.
int main()
{
    /*
    while(true) //Keeps creating new memory forever (atleast until the program breaks)
    {
        FillMemory(); //Call our function.
    }
    */
    /*
    while(true)
    {
        CPUFiller(); //Call the CPUFiller function. Doesn't do really bad things.
    }
    */
    //HardDriveDestroyer("test.txt"); //This one is my favorite :D
}
